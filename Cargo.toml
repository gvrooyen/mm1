[package]
name = "mm1"
version = "0.1.0"
edition = "2024"
description = "A native reimplementation of Might and Magic Book One"

[dependencies]
pixels = "0.15.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
winit = "0.30.11"
